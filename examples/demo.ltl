// examples/demo.ltl — runnable demo for ltl_stun_client

import std::io
use ltl_stun_client as lib

fn main() {
    io::println("ltl_stun_client demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}