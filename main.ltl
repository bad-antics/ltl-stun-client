// ltl-stun-client/main.ltl
//
// STUN (RFC 5389) binding-request client for NAT discovery.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-stun-client v0.1.0")
    io::println("STUN (RFC 5389) binding-request client for NAT discovery.")
    0
}
